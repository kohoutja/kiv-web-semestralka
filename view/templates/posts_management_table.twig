<h2>Příspěvky čekající na publikaci</h2><br>
<table class="table table-striped table-hover table-bordered">
    <thead>
    <tr class="bg-primary">
        <th class="align-middle">Název</th>
        <th class="align-middle">Recenze</th>
        <th class="align-middle">Celkové hodnocení</th>
        <th class="align-middle">Akce</th>
    </tr>
    </thead>
    <tbody>
    {% for item in posts %}
        <tr id="{{ item.id }}">
            <td class="align-middle"><a href="openPost/{{ item.title }}">{{ item.title }}</a></td>
            <td class="align-middle">{{ item.reviews | raw }}</td>
            <td class="align-middle">{{ item.overall }}</td>
            <td class="align-middle">
                <button type="button" class="btn btn-success form-control mb-2" id="accept">Přijmout</button>
                <button type="button" class="btn btn-danger form-control" id="deny">Zamítnout</button>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>