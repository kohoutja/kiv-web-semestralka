<h2>Seznam zveřejněných příspěvků</h2><br>
<table class="table table-bordered table-striped table-hover">
    <thead>
    <tr class="bg-primary">
        <th>Název</th>
        <th>Autor</th>
    </tr>
    </thead>
    <tbody>
    {% for item in published %}
        <tr>
            <td><a href="openPost/{{ item.title }}">{{ item.title }}</a></td>
            <td>{{ item.fullName }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>