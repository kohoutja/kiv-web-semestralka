<h2>Příspěvky čekající na přidělení recenzentovi</h2><br>
<table class="table table-striped table-hover">
    <thead>
    <tr class="bg-primary">
        <th>Název</th>
        <th>Autor</th>
    </tr>
    </thead>
    <tbody>
    {% for item in posts %}
        <tr>
            <td><a href="openPost/{{ item.title }}">{{ item.title }}</a></td>
            <td>{{ item.fullName }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>